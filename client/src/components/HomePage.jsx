import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="chiact" subtitle="A boilerplate for Go and React" />
  </Card>
);

export default HomePage;
